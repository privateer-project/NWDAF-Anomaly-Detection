FROM python:3.11-slim

WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir kafka-python

# Copy the anonymizer script
COPY anonymizer.py /app/

# Run the anonymizer
CMD ["python", "/app/anonymizer.py"]